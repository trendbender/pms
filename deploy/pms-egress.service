[Unit]
Description=PMS container egress firewall (DOCKER-USER)
After=docker.service
Requires=docker.service
[Service]
Type=oneshot
ExecStart=/usr/local/sbin/pms-egress.sh
RemainAfterExit=yes
[Install]
WantedBy=multi-user.target
